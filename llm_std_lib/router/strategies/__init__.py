"""
Routing strategy implementations for llm_std_lib.

Each strategy encapsulates a single model-selection algorithm and exposes
a common interface consumed by :class:`~llm_std_lib.router.model_router.ModelRouter`.
"""
